---
const brand = "Amicbridge";
---
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{brand} — Everyday Comfort</title>
    <meta name="description" content="Amicbridge makes comfortable, thoughtfully designed underwear for women." />
    <style>
      :root {
        --bg: #f7f6f4;
        --text: #1f1f1f;
        --muted: #6b6b6b;
        --accent: #111111;
      }

      * {
        box-sizing: border-box;
      }

      body {
        margin: 0;
        font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI",
          Roboto, Ubuntu, sans-serif;
        background: var(--bg);
        color: var(--text);
        line-height: 1.6;
      }

      header {
        padding: 2rem 3rem;
        display: flex;
        justify-content: space-between;
        align-items: center;
      }

      header h1 {
        font-size: 1.2rem;
        letter-spacing: 0.05em;
        text-transform: uppercase;
        margin: 0;
      }

      nav a {
        margin-left: 1.5rem;
        text-decoration: none;
        color: var(--text);
        font-size: 0.9rem;
      }

      main {
        max-width: 1100px;
        margin: 0 auto;
        padding: 4rem 3rem;
      }

      .hero {
        max-width: 600px;
      }

      .hero h2 {
        font-size: 3rem;
        line-height: 1.2;
        margin-bottom: 1rem;
      }

      .hero p {
        color: var(--muted);
        font-size: 1.1rem;
        margin-bottom: 2rem;
      }

      .hero a {
        display: inline-block;
        padding: 0.8rem 1.6rem;
        border: 1px solid var(--accent);
        text-decoration: none;
        color: var(--accent);
        font-size: 0.9rem;
      }

      section {
        margin-top: 6rem;
      }

      .features {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
        gap: 2rem;
      }

      .feature h3 {
        margin-bottom: 0.5rem;
        font-size: 1.1rem;
      }

      .feature p {
        color: var(--muted);
        font-size: 0.95rem;
      }

      footer {
        margin-top: 6rem;
        padding: 2rem 3rem;
        border-top: 1px solid #ddd;
        font-size: 0.85rem;
        color: var(--muted);
      }
    </style>
  </head>

  <body>
    <header>
      <h1>{brand}</h1>
      <nav>
        <a href="#">Shop</a>
        <a href="#">About</a>
        <a href="#">Contact</a>
      </nav>
    </header>

    <main>
      <div class="hero">
        <h2>Underwear designed for real life</h2>
        <p>
          Amicbridge creates thoughtfully made underwear for women who value
          comfort, simplicity, and quiet confidence.
        </p>
        <a href="#">Explore the collection</a>
      </div>

      <section>
        <div class="features">
          <div class="feature">
            <h3>Comfort first</h3>
            <p>
              Soft, breathable fabrics with cuts that respect real bodies—not
              trends.
            </p>
          </div>

          <div class="feature">
            <h3>Thoughtful design</h3>
            <p>
              Waistbands that don’t roll. Seams that don’t shout. Details that
              matter.
            </p>
          </div>

          <div class="feature">
            <h3>Everyday quality</h3>
            <p>
              Built to be worn, washed, and trusted again and again.
            </p>
          </div>
        </div>
      </section>
    </main>

    <footer>
      © {new Date().getFullYear()} {brand}. All rights reserved.
    </footer>
  </body>
</html>

