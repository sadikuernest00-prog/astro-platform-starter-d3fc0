import { useMemo, useState } from "react";
import "./styles.css";

const base = import.meta.env.BASE_URL;
// helper: always build correct URL (works on / and subfolders)
const img = (path) => `${base}${path.replace(/^\/+/, "")}`;

// ✅ KEEP your existing products list (paste your full array here)
const products = [
  {
    id: "set-black",
    name: "Black Satin-Look Cami & Shorts Set",
    price: 400,
    image: "/images/set-black.png",
    colors: ["Black"],
    sizes: ["S", "M", "L"],
    defaultColor: "Black",
  },
  {
    id: "set-white",
    name: "White Satin-Look Cami & Shorts Set",
    price: 400,
    image: "/images/set-white.png",
    colors: ["White"],
    sizes: ["S", "M", "L"],
    defaultColor: "White",
  },
  {
    id: "nude-slip",
    name: "Nude Satin-Look Slip Dress",
    price: 400,
    image: "/images/nude-slipp-dress.png",
    colors: ["Nude"],
    sizes: ["S", "M", "L"],
    defaultColor: "Nude",
  },
  {
    id: "white-slip",
    name: "White Satin-Look Slip Dress",
    price: 499,
    image: "/images/white-slip-dress.png",
    colors: ["White"],
    sizes: ["S", "M", "L"],
    defaultColor: "White",
  },
  {
    id: "black-chemise",
    name: "Black Satin-Look Chemise Dress",
    price: 499,
    image: "/images/black-chemise.png",
    colors: ["Black"],
    sizes: ["S", "M", "L"],
    defaultColor: "Black",
  },
  // ...continue your list
];

const formatPrice = (n) => `${n} kr`;

export default function App() {
  const [query, setQuery] = useState("");
  const [size, setSize] = useState("All");
  const [sort, setSort] = useState("featured");

  const filtered = useMemo(() => {
    let list = [...products];

    if (query.trim()) {
      const q = query.toLowerCase();
      list = list.filter((p) => p.name.toLowerCase().includes(q));
    }

    if (size !== "All") {
      list = list.filter((p) => p.sizes?.includes(size));
    }

    if (sort === "price-asc") list.sort((a, b) => a.price - b.price);
    if (sort === "price-desc") list.sort((a, b) => b.price - a.price);

    return list;
  }, [query, size, sort]);

  return (
    <div className="page">
      <div className="topbar">
        <div className="container topbar__inner">
          <span>Free shipping over 800 kr</span>
          <span className="dot" />
          <span>30-day easy returns</span>
          <span className="dot" />
          <span>Designed for real life</span>
        </div>
      </div>

      <header className="header">
        <div className="container header__inner">
          <a className="brand" href="#">
            Amicbridge
          </a>

          <nav className="nav" aria-label="Primary">
            <a href="#shop">Shop</a>
            <a href="#materials">Materials</a>
            <a href="#fit">Fit Guide</a>
            <a href="#story">Our Story</a>
          </nav>

          <div className="header__actions">
            <button className="iconBtn" aria-label="Search">
              ⌕
            </button>
            <button className="iconBtn" aria-label="Account">
              ⌂
            </button>
            <button className="iconBtn" aria-label="Cart">
              ⊏⊐
              <span className="badge" aria-label="Items in cart">
                0
              </span>
            </button>
          </div>
        </div>
      </header>

      <main>
        <section className="hero" id="story">
          <div className="container hero__inner">
            <div className="hero__copy">
              <p className="eyebrow">Thoughtfully made • Minimal seams • All-day comfort</p>

              <h1 className="hero__title">
                Underwear designed <span className="em">for real life</span>
              </h1>

              <p className="hero__subtitle">
                Amicbridge creates thoughtfully made underwear for women who value comfort,
                simplicity, and quiet confidence.
              </p>

              <div className="hero__cta">
                <a className="btn btn--primary" href="#shop">
                  Shop bestsellers
                </a>
                <a className="btn btn--ghost" href="#materials">
                  Learn about fabrics
                </a>
              </div>

              <div className="trust">
                <div className="trust__item">
                  <span className="trust__icon">✓</span>
                  <div>
                    <strong>Soft-touch satin look</strong>
                    <div className="muted">Premium feel, easy care</div>
                  </div>
                </div>
                <div className="trust__item">
                  <span className="trust__icon">✓</span>
                  <div>
                    <strong>Comfort-first fit</strong>
                    <div className="muted">No digging, no fuss</div>
                  </div>
                </div>
                <div className="trust__item">
                  <span className="trust__icon">✓</span>
                  <div>
                    <strong>Made to last</strong>
                    <div className="muted">Quality stitching & finish</div>
                  </div>
                </div>
              </div>
            </div>

            <div className="hero__media" aria-hidden="true">
              <div className="mediaCard">
                <div className="mediaCard__glow" />
                <img
                  className="mediaCard__img"
                  src={img(products[0]?.image || "/images/set-black.png")}
                  alt=""
                  loading="eager"
                />
              </div>

              <div className="floatingTag floatingTag--top">
                <strong>New</strong> Satin-look sets
              </div>
              <div className="floatingTag floatingTag--bottom">
                <strong>Best seller</strong> Slip dresses
              </div>
            </div>
          </div>
        </section>

        <section className="shop" id="shop">
          <div className="container">
            <div className="sectionHead">
              <div>
                <h2>Shop</h2>
                <p className="muted">Bestsellers, sets, slips, and comfort bras.</p>
              </div>

              <div className="controls" role="group" aria-label="Product filters">
                <div className="field">
                  <label className="srOnly" htmlFor="q">
                    Search
                  </label>
                  <input
                    id="q"
                    value={query}
                    onChange={(e) => setQuery(e.target.value)}
                    placeholder="Search products…"
                  />
                </div>

                <div className="field">
                  <label className="srOnly" htmlFor="size">
                    Size
                  </label>
                  <select id="size" value={size} onChange={(e) => setSize(e.target.value)}>
                    <option>All</option>
                    <option>S</option>
                    <option>M</option>
                    <option>L</option>
                  </select>
                </div>

                <div className="field">
                  <label className="srOnly" htmlFor="sort">
                    Sort
                  </label>
                  <select id="sort" value={sort} onChange={(e) => setSort(e.target.value)}>
                    <option value="featured">Featured</option>
                    <option value="price-asc">Price: Low → High</option>
                    <option value="price-desc">Price: High → Low</option>
                  </select>
                </div>
              </div>
            </div>

            <div className="grid">
              {filtered.map((p) => (
                <ProductCard key={p.id} p={p} img={img} />
              ))}
            </div>
          </div>
        </section>

        <section className="info" id="materials">
          <div className="container info__inner">
            <div className="infoCard">
              <h3>Materials that behave</h3>
              <p className="muted">
                Smooth, breathable, and designed to move. Less “lingerie costume”, more “everyday
                luxury”.
              </p>
              <a className="textLink" href="#fit">
                See fit guide →
              </a>
            </div>
            <div className="infoCard">
              <h3>Quiet confidence</h3>
              <p className="muted">
                Minimal seams, comfortable cuts, and colors that match real wardrobes.
              </p>
              <a className="textLink" href="#shop">
                Shop staples →
              </a>
            </div>
          </div>
        </section>

        <footer className="footer">
          <div className="container footer__inner">
            <div>
              <div className="brand brand--small">Amicbridge</div>
              <p className="muted">Comfort, simplicity, and quiet confidence.</p>
            </div>
            <div className="footerLinks">
              <a href="#shop">Shop</a>
              <a href="#materials">Materials</a>
              <a href="#fit">Fit Guide</a>
              <a href="#story">Our Story</a>
            </div>
          </div>
        </footer>
      </main>
    </div>
  );
}

function ProductCard({ p, img }) {
  const [pickedSize, setPickedSize] = useState(p.sizes?.[0] || "M");

  return (
    <article className="card">
      <a className="card__media" href="#" aria-label={`View ${p.name}`}>
        <img src={img(p.image)} alt={p.name} loading="lazy" />
      </a>

      <div className="card__body">
        <div className="card__top">
          <h3 className="card__title">{p.name}</h3>
          <div className="card__price">{formatPrice(p.price)}</div>
        </div>

        <div className="card__meta">
          <span className="pill">{p.defaultColor}</span>
          <span className="pill">{p.sizes?.length || 0} sizes</span>
        </div>

        <div className="card__actions">
          <select
            className="miniSelect"
            value={pickedSize}
            onChange={(e) => setPickedSize(e.target.value)}
            aria-label="Choose size"
          >
            {(p.sizes || []).map((s) => (
              <option key={s}>{s}</option>
            ))}
          </select>

          <button className="btn btn--primary btn--small" type="button">
            Add to cart
          </button>
        </div>
      </div>
    </article>
  );
}

